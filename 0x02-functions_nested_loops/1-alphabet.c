#include <string.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description: 'function prints the alphabet'
 * Return: void
 */
int main(void)
{
char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
	{putchar(ch);
	}
	putchar('\n');
}
