#include <stdio.h>
/**
* main - Entry point
* Description: 'Code prints the alphabet'
* Return: Always 0 (Success) 
*/
int main(){
char alphabet = 'a';
while (alphabet <= 'z')
{putchar(alphabet);
}
alphabet++;
return (0);
}
