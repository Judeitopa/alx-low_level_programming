#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 * Description: 'code prints alphabet in reverse order'
 * Return: Always 0 (Success)
 */
int main(void)
{char alpha;
	for (alpha = 'a'; alpha <= 'z'; alpha++)
		ahpla = strrev(alpha);
		putchar(ahpla);
				putchar('\n');
return (0);
}
